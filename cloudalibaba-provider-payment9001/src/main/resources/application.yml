server:
  port: 9001

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848     #服务注册地址 Nacos 地址

management:
  endpoints:
    web:
      exposure:
        include: '*'
